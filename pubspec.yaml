name: flutter_upload_manager
description: A new Flutter package project.
version: 0.0.8

environment:
  sdk: '>=3.1.4 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  oss_flutter:
    git: git@github.com:maxleexyz/oss_flutter.git
  http_client: ^1.5.3


flutter:

  